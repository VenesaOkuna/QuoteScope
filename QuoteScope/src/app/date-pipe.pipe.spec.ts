import { DatePipePipe } from '/home/venesa/Documents/school_projects/CORE/QuoteScope/QuoteScope/src/app/date-pipe.pipe';

describe('DatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DatePipePipe();
    expect(pipe).toBeTruthy();
  });
});