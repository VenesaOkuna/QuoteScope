import { HighlightQouteDirective } from '/home/venesa/Documents/school_projects/CORE/QuoteScope/QuoteScope/src/app/highlight-quote.directive';

describe('HighlightQouteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightQouteDirective();
    expect(directive).toBeTruthy();
  });
});